<style>
  /* ↓ Shrink overall card footprint (about 50% smaller height) */
  #feature-grid .card {
    @apply scale-[0.9] md:scale-[0.95]; /* visually smaller */
    transform-origin: top center;
  }

  /* ↓ Tighten spacing inside cards (if your <Card> component has inner padding) */
  #feature-grid .card > * {
    @apply py-1 sm:py-2;
  }

  /* ↓ Limit height to avoid tall uneven cards */
  #feature-grid > div {
    @apply h-[180px] sm:h-[220px] lg:h-[240px];
  }

  /* ↓ Keep equal heights & clean transitions */
  #feature-grid > div,
  #feature-grid .card {
    @apply flex flex-col justify-between;
  }

  /* Active filter button */
  #feature-filters .is-active {
    box-shadow: 0 0 0 1px theme('colors.primary.500 / 0.6');
  }

  /* Instant fade (snappy show/hide) */
  #feature-grid > div {
    opacity: 1;
    transition: opacity 0.15s ease-out;
  }
  #feature-grid > div.is-hidden {
    opacity: 0;
    pointer-events: none;
  }
</style>